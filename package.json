{
  "name": "noh-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,user,admin -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/user\" \"npm run dev --prefix frontend/admin\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:user": "npm run dev --prefix frontend/user",
    "dev:admin": "npm run dev --prefix frontend/admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
